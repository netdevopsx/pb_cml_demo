---
- name: Setup NTP Service                                                                                                                                                       
  hosts: all 
  gather_facts: no                                                                                                                                                                       
  tasks:
  - name: "Set NTP Server"
    cisco.ios.ios_ntp:
      server: "{{ item }}"
      state: present
    loop: "{{ system.ntp.servers }}"